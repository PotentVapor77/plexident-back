version: 1.0
runtime: python311
build:
  commands:
    pre_build:
      - pip install --upgrade pip  # Opcional, si separas
    build:
      - pip install --no-cache-dir -r requirements.txt
run:
  runtime-version: 3.11.10
  command: sh start.sh
  network:
    port: 8000